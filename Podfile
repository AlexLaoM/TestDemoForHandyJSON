platform :ios, '10.0'

use_frameworks!
targetsArray = ['DemoForHandyJSON']
targetsArray.each do |t|
    target t do
      pod 'HandyJSON','~> 5.0.1'
      pod 'Dynatrace', '~> 7.0'
      pod 'AlamofireObjectMapper', '~> 5.2'

    end
end
